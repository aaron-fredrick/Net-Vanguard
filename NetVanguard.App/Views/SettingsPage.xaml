<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="NetVanguard.App.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:NetVanguard.App.Helpers"
    mc:Ignorable="d" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Application Settings" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,24" />

        <StackPanel Grid.Row="1" Spacing="24">
            
            <Border Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Appearance" Style="{StaticResource SubtitleTextBlockStyle}" />
                    
                    <ComboBox Header="Application Theme" 
                              ItemsSource="{x:Bind ViewModel.AvailableThemes, Mode=OneWay}" 
                              SelectedItem="{x:Bind ViewModel.SelectedThemeString, Mode=TwoWay}"
                              Width="200"
                              helpers:CursorExtensions.Cursor="Hand"/>
                              
                    <TextBlock Text="Changes to the application theme are saved instantly and apply across the entire application." 
                               Foreground="Gray" FontSize="12" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="About Net-Vanguard" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Version: 0.1.00-dev" />
                    <TextBlock Text="License: PolyForm Noncommercial 1.0.0" />
                    <TextBlock Text="An advanced, high-performance network tracking and Windows Firewall management utility built using modern C# 12 and .NET 8." 
                               TextWrapping="Wrap" Foreground="Gray" />
                </StackPanel>
            </Border>
            
        </StackPanel>
    </Grid>
</Page>
