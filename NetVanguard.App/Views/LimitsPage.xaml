<Page
    x:Class="NetVanguard.App.Views.LimitsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NetVanguard.Core.Models"
    xmlns:converters="using:NetVanguard.App.Converters"
    mc:Ignorable="d" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,24" Spacing="16">
            <TextBlock Text="Network Limits" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center" />
            <Button Style="{StaticResource AccentButtonStyle}" Click="AddLimit_Click" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE710;" FontSize="14"/>
                    <TextBlock Text="Add New Limit"/>
                </StackPanel>
            </Button>
            <Button Click="Refresh_Click" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" ToolTipService.ToolTip="Refresh Rules">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE72C;" FontSize="14"/>
                    <TextBlock Text="Refresh Rules" VerticalAlignment="Center" FontSize="12" Foreground="Gray"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Global Constraints" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,16"/>
                    
                    <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.ActiveLimits, Mode=OneWay}" 
                              SelectedItem="{x:Bind ViewModel.SelectedLimit, Mode=TwoWay}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:TrafficLimitConfiguration">
                                <StackPanel Padding="8" Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind TargetName, Mode=OneWay}" FontWeight="SemiBold"/>
                                        <Border Background="{ThemeResource SystemAccentColor}" CornerRadius="4" Padding="4,2">
                                            <TextBlock Text="{Binding TargetType}" FontSize="10" Foreground="White" FontWeight="Bold"/>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="Quotas &amp; Bandwidth active." FontSize="12" Foreground="Gray"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>

            <Border Grid.Column="1" Width="1" Background="{ThemeResource SystemControlForegroundBaseLowBrush}" Margin="8,0" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>

            <Border Grid.Column="2" Background="{ThemeResource SystemControlBackgroundAltHighBrush}" CornerRadius="8" Padding="20" Width="340"
                    Visibility="{x:Bind ViewModel.IsDetailPaneVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Rule Inspection" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="Gray" Margin="0,0,0,16"/>
                    
                    <StackPanel Grid.Row="1" Spacing="8" Margin="0,0,0,24">
                        <TextBlock Text="{x:Bind ViewModel.DetailTargetName, Mode=OneWay}" FontSize="24" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.DetailTargetType, Mode=OneWay}" FontSize="14" Foreground="{ThemeResource SystemAccentColor}"/>
                    </StackPanel>
                    
                    <Border Grid.Row="2" Background="{ThemeResource SystemControlBackgroundAltMediumBrush}" CornerRadius="6" Padding="16" Margin="0,0,0,24">
                        <StackPanel Spacing="16">
                            <StackPanel>
                                <TextBlock Text="Total Bytes Blocked" FontSize="12" Foreground="Gray"/>
                                <TextBlock Text="{x:Bind ViewModel.DetailTotalBytesBlocked, Mode=OneWay}" FontSize="20" FontWeight="SemiBold" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Data Quota Constraint" FontSize="12" Foreground="Gray"/>
                                <TextBlock Text="{x:Bind ViewModel.DetailDataQuota, Mode=OneWay}" FontSize="16" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Bandwidth Throttle (Bps)" FontSize="12" Foreground="Gray"/>
                                <TextBlock Text="{x:Bind ViewModel.DetailThrottle, Mode=OneWay}" FontSize="16" Margin="0,4,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    
                    <Button Grid.Row="4" Foreground="White" Background="#C62828" HorizontalAlignment="Stretch" Padding="8" Click="DeleteLimit_Click">
                        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE74D;" FontSize="14"/>
                            <TextBlock Text="Destroy Limit Constraint" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
